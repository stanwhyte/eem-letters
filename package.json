{
  "name": "eem-letters",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:init": "wrangler d1 execute eem-letters --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
